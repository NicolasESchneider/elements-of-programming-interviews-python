# given a pivot_index and an array of integers, partition the array into 3 sections in the following order:
## numbers smaller than the integer at the pivot_index
## numbers equal to the integer at the pivot_index
## numers greater than the integer at the pivot_index

def dutch_flag_partition(array, pivot_index):
    
